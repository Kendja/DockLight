#!/bin/sh
set -e
mkdir -p /usr/share/DockLight/data
# Will allow all users read and write access to all files and folders within that directory
chmod 0777 /usr/share/DockLight/data
chmod 0777 /usr/share/DockLight/docklight.config

